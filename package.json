{
  "name": "cyber-story-lab",
  "version": "0.6.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "check": "node --check public/app.js && node --check public/free-mode.js && node --check src/worker.js",
    "test": "node tests/worker.test.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
